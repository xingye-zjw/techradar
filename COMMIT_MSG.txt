chore(repo): tidy up folder structure (4 targeted cleanups, 0 functional change)

## Scope: 4 agreed housekeeping tasks from "整理当前文件夹" — NO code/content logic changes

### 1) 🗂️ docs/ 3-tier archive (16 docs reorganized + 2 top-level drafts moved)
RATIONALE: docs/ top-level had mixed 5 core deliverables + 14 date-prefixed sprint/spec files + 2 scattered drafts; name "superpowers" not intuitive.
- DELETE obsolete container docs/superpowers/{plans,specs}/ (empty after move)
- MOVE 6 sprint plans     docs/superpowers/plans/*.md → docs/sprints/*-sprint.md (add suffix -sprint.md to disambiguate from specs)
- MOVE 8 design specs    docs/superpowers/specs/*.md → docs/specs/ (keep names, 8 files incl. sidebar-glossary + 2 techradar foundations + 5 content-feature designs)
- MOVE 2 top-level drafts bilibili-video-suggestions.md + content-spec.md → docs/drafts/*-draft.md (add suffix -draft.md)
- RESULT: docs/ top-level only contains the 5 core parallel-processing deliverables (architecture / modules-inventory / new-content-scope / parallel-processing-plan / team-resources)

### 2) 🧪 scripts/_deprecated/ quarantine (5 zombie scripts segregated without deletion)
RATIONALE: same-function duplicates / temp-named / .cp.py copy-artifacts were misleadingly scattered among live scripts.
MOVE → scripts/_deprecated/ (retained for audit trail, not deleted):
- add_nlp_nodes.py                       # duplicate of add-nlp-nodes.py (different naming convention)
- _hotfix_yaml_relatedIntel_syntax.cp.py # copy-artifact (.cp.py suffix); fully replaced by _fix_all_yaml_and_renumber_v2.py
- temp-check-desc.py                     # one-off temp- prefixed debug
- fix-resource-types2.py                 # v2 duplicate vs fix-resource-types.py (never validated as needed)
- fix-multiline2.py                      # v2 duplicate vs fix-multiline.py (same)

### 3) 📐 soft-classify 91 scripts via scripts/README.md (0 file moves = 0 import/config breakage)
RATIONALE: 88/91 Python scripts `import config` from scripts/config.py at repo root; physical moves into subdirs would ImportError. Chose soft-classification per user's Option B (0-risk).
NEW scripts/README.md (250-line live index):
- TOP: 3 ANCHOR WARNINGS for scripts that MUST STAY at root
  - prebuild-search-index.mjs  → package.json scripts.prebuild (tsx scripts/prebuild-search-index.mjs)
  - audit-content3.py          → .github/workflows/ci.yml line 37 (hard-coded)
  - config.py                  → imported by 88+ scripts (import config / from config import)
- 6-category index of ALL 91 active scripts:
  1) audit/check/verify/quality (17)  [read-only; CI & local checks]
  2) fix/normalize/migrate/_hotfix (27) [mutating content/lib; warning pre-commit]
  3) fill-* (25)  [roadmap-day content batch generators; sprints 1-14]
  4) add/generate/expand/ensure (12) [data INCREMENT not mutation]
  5) analyze/list/find/debug/temp/track (12) [read-only statistics]
  6) roadmap specials
- Recommended 4-step pre-sprint flow (audit → analyze → fix → CI-verify)
- New-script naming convention table (prefix → mutating? → category)
- Deprecated-quarantine table with each script's reason + replacement
- 3-sprint unused → safe deletion policy

### 4) 🗑️ Remove obsolete pitfall JSON template + sync templates/README.md
RATIONALE: Pitfall module was merged into content/intel/ (slugs 090-162, 174-182 migrated → 190-198). Independent content/pitfall/pitfalls.json + its template no longer existed; leftover would mislead new contributors.
- DELETE templates/pitfall/pitfalls.json.template
- DELETE now-empty templates/pitfall/ directory
- UPDATE templates/README.md (3 places):
  (a) TOC: remove pitfall/ line; re-label intel template "含 Pitfall 类情报"
  (b) SECTION "3 添加踩坑记录": DELETED (was pointing at non-existent content/pitfall/pitfalls.json); merged into Section 1 ("添加新情报 / 踩坑记录") with clear pitfall-* slug-prefix naming rule + mandatory category field referencing ContentCategory enum in lib/content-types.ts
  (c) SECTION "踩坑分类": REWROTE table from obsolete 6-row Chinese-only labels → 23 pitfall-* CONTENT-CATEGORY SLUGS (e.g. pitfall-dl-training / pitfall-gpu-cuda / pitfall-llm-app / pitfall-rag ...) with Chinese meaning; added migration note "v0.1 起 Pitfall 合并到 Intel" so new contributors understand pitfall-category usage

## NON-CHANGES (intentionally left alone to avoid risk)
- lib/terms.json (50 terms, {terms:[...]}) vs content/glossary/terms.json (58 terms, list[]) — parallel term stores with DIFFERENT shapes and different consumers (lib/terms.ts vs lib/glossary.ts); flagged but deferred to a dedicated feature PR to unify
- scripts/validate-content.ts referenced by package.json + ci.yml → does not exist (CI uses continue-on-error fallback); deferred to separate follow-up

## VERIFICATION (all 5 gates GREEN, identical health vs pre-tidy commit b03e3bb)
1. tsc --noEmit                    ✅ 0 errors
2. npm run lint:ci                ✅ 0 errors · 59 pre-existing warnings only
3. npx vitest run                 ✅ 15/15 files · 93/93 tests passed (same 4 vitest stdout noise = expected security-test logs, not failures)
4. python scripts/check-constants.py  ✅
                                    NODE_NAMES    0 missing / 0 extra
                                    INTEL_LINKS   0 missing
                                    content/intel 0 missing files
                                    tools.json    0 missing tool ids (70 list matches)
5. npx next build                 ✅ exit 0  (full static SSG export)
                                    737 roadmap/[node]/day/[day]  SSG pages
                                    188 intel slugs (184 listed + 4 preview)  prerendered
                                    59  glossary slugs  prerendered
                                    18  practice slugs  prerendered
                                    71  toolbox ids    prerendered
                                    (2 Next metadata themeColor warnings are pre-existing and unrelated to docs/scripts/templates)
